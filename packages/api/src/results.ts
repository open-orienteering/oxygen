/**
 * Result computation — re-exports from @oxygen/shared.
 * Server and client use the same logic.
 */
export {
  computePosition,
  computeClassPlacements,
  type ClassRunnerForPosition,
  type PositionResult,
  type RunnerForPlacement,
  type PlacementResult,
} from "@oxygen/shared";
