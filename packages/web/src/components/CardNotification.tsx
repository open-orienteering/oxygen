/**
 * Card notification banner — being re-ported. Pending the device-manager
 * + punch-matcher re-port. Returns a no-op component so callers compile.
 */
export function CardNotification() {
  return null;
}
