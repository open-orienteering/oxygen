/**
 * Backup punches page — being re-ported. Surfaces SI-station backup
 * memory imports. Pending the deeper port.
 */
export function BackupPunchesPage() {
  return (
    <div className="p-6">
      <h1 className="mb-2 text-xl font-semibold">Backup punches</h1>
      <p className="text-sm text-gray-600 dark:text-gray-300">
        The backup-punch admin view is being re-ported against the new
        PostgreSQL schema. Coming back online shortly.
      </p>
    </div>
  );
}

export default BackupPunchesPage;
