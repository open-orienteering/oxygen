/**
 * Card readout page — being re-ported. Pending the punch-matcher port.
 */
export function CardReadout() {
  return (
    <div className="p-6">
      <h1 className="mb-2 text-2xl font-semibold">Card readout</h1>
      <p className="text-sm text-amber-700 dark:text-amber-300">
        The standalone card-readout view is being re-ported against the
        new PostgreSQL schema. Coming back online shortly.
      </p>
    </div>
  );
}

export default CardReadout;
