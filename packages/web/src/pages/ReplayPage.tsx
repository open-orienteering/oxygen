/**
 * Replay page — being re-ported. Punch-matcher driven leg replay.
 */
export function ReplayPage() {
  return (
    <div className="p-6">
      <h1 className="mb-2 text-xl font-semibold">Replay</h1>
      <p className="text-sm text-gray-600 dark:text-gray-300">
        The replay view is being re-ported against the new PostgreSQL
        schema. Coming back online shortly.
      </p>
    </div>
  );
}

export default ReplayPage;
